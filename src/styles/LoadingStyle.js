const LoadingStyle = function(theme){
    return {
        main: {
            height: '50px',
            marginTop: '20px',
            color: theme.palette.text.disabled
        }
    }
}

export default LoadingStyle